<!DOCTYPE html>
<html>
  <head>
    <title>Movies page</title>
    <link rel="stylesheet" type="text/css" href="styles.css">
  </head>
  <body>
    <article class="container">
      <h3>Movie search results ({{pages}} pages) for "{{query}}" page {{page}}</h3>
      <!--<div class="pagination">
        {{#for 0 pages 1 1}}
          <a href="{{concat '/getmovies?query=' ../query '&page=' this}}">{{this}}</a>
        {{/for}}
      </div>-->
      <form method="post" action="/getmovies">
        <input type="text" name="query" placeholder="Search..." value="{{query}}" />
        <select name="page">
          <option value="1">Page...</option>
          {{#for 0 pages 1 1}}
            <option value="{{this}}">{{this}}</option>
          {{/for}}
        </select>
        <button type="submit">Go</button>
      </form>
      <ul class="flex-container wrap">
        {{#each Search}}
          <li class="flex-item">
            <div class="image">
              {{#ifcond Poster 'N/A'}}
                <img src="https://bit.ly/2F3uCeS">
              {{else}}
                <a href="https://www.imdb.com/title/{{imdbID}}" target="_blank">
                  <img src="{{Poster}}">
                </a>
              {{/ifcond}}
            </div>
            <div class="title">
              <h3>{{Title}}, {{Year}}</h3>
            </div>
          </li>
        {{/each}}
      </ul>
    </article>
    <footer class="text-center text-muted p-20">&copy; Copyright 2020 <a href="https://rolandlevy.co.uk" class="new-tab-icon" target="_blank">see more of my projects</a></footer>
    <script>
      // document.addEventListener('DOMContentLoaded', => {
      //   const thisYear = (new Date()).getFullYear();
      //   const div = document.querySelector('.full-year');
      //   div.innerHtml = thisYear;
      // });
    </script>
  </body>
</html>